name: flex_sidebar
description: "A highly customizable sidebar and drawer widget for Flutter projects, supporting Android, iOS, Web, and Desktop."
version: 1.0.0
homepage:

environment:
  sdk: ^3.6.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
